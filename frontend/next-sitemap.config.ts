
const config = {
  siteUrl: 'https://swastify.life',
  generateRobotsTxt: true, // Optional: Generates robots.txt as well
  exclude: ['/404', '/password-protected'], // Exclude any pages you don't want in the sitemap
};

export default config;
